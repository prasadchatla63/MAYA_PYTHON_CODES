def sum_of_divisors(num):
    s = 0
    for i in range(1, num):
        if num % i == 0:
            s += i
    return s

n = int(input())
m = int(input())

if sum_of_divisors(n) == m and sum_of_divisors(m) == n:
    print("Amicable")
else:
    print("Not Amicable")
